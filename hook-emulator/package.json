{
  "name": "hook-emulator",
  "scripts": {
    "lint": "eslint ./hook-emulator.js",
    "build:wrapper": "gcc -m32 ./c_wrappers/main.c ../src/evernode.c ../lib/emulatorapi.c -o ./c_wrappers/hook-wrapper",
    "build": "npm run build:wrapper && npm run lint && ncc build hook-emulator.js --minify -o dist",
    "start": "npm run build:wrapper && node hook-emulator.js"
  },
  "dependencies": {
    "evernode-js-client": "0.4.14",
    "jsonwebtoken": "8.5.1",
    "ripple-address-codec": "4.2.0",
    "ripple-binary-codec": "1.3.0",
    "sqlite3": "5.0.2"
  },
  "devDependencies": {
    "eslint": "8.3.0"
  }
}
