{
  "name": "account-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/eslint ./index.js",
    "build:self": "npm run lint && ncc build index.js --minify -o dist",
    "build:emulator": "npm --prefix ../../hook-emulator run build",
    "build:prepare": "([ ! -d ./dist/hook-emulator ] || rm -r ./dist/hook-emulator) && cp -r ../../hook-emulator/dist ./dist/hook-emulator && mkdir ./dist/hook-emulator/c-wrappers && cp -r ../../hook-emulator/c-wrappers/hook-wrapper ./dist/hook-emulator/c-wrappers/ && cp -r ../../hook-emulator/sec ./dist/hook-emulator/sec",
    "build": "npm run build:self && npm run build:emulator && npm run build:prepare",
    "start": "npm run build && node dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "evernode-js-client": "0.4.12",
    "ripple-address-codec": "4.2.0"
  },
  "devDependencies": {
    "eslint": "8.3.0"
  }
}